package leetcode.p0189

/**
 * # 189. Rotate Array
 *
 * Given an integer array `nums`, rotate the array to the right by `k` steps, where `k` is
 * non-negative.
 *
 * Examples:
 * - Input: `nums = [1,2,3,4,5,6,7]`, `k = 3`. Output: `[5,6,7,1,2,3,4]`.
 *   Explanation: Rotating right by one step produces `[7,1,2,3,4,5,6]`, by two steps produces
 *   `[6,7,1,2,3,4,5]`, and by three steps produces `[5,6,7,1,2,3,4]`.
 * - Input: `nums = [-1,-100,3,99]`, `k = 2`. Output: `[3,99,-1,-100]`.
 *
 * Constraints:
 * - `1 <= nums.length <= 10^5`.
 * - `-2^31 <= nums[i] <= 2^31 - 1`.
 * - `0 <= k <= 10^5`.
 *
 * Follow-up: Can you solve this problem in-place with `O(1)` extra space?
 *
 * [LeetCode 189: Rotate Array](https://leetcode.com/problems/rotate-array/)
 */
class Solution {
    fun rotate(nums: IntArray, k: Int) {
        TODO("Implement solution")
    }
}