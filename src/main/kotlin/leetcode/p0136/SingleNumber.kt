package leetcode.p0136

/**
 * # 136. Single Number
 *
 * Given a non-empty array of integers `nums`, every element appears twice except for one element
 * that appears exactly once. Find and return the element that appears only once.
 *
 * You must implement a solution with linear runtime complexity and use only constant extra
 * space.
 *
 * Examples:
 * - Input: `nums = [2, 2, 1]`. Output: `1`.
 * - Input: `nums = [4, 1, 2, 1, 2]`. Output: `4`.
 * - Input: `nums = [1]`. Output: `1`.
 *
 * Constraints:
 * - `1 <= nums.length <= 3 * 10^4`.
 * - `-3 * 10^4 <= nums[i] <= 3 * 10^4`.
 * - Every element in `nums` appears twice except for one element, which appears once.
 *
 * Follow-up: Can you implement a solution with a linear runtime complexity and without using
 * extra memory?
 *
 * [LeetCode 136: Single Number](https://leetcode.com/problems/single-number/)
 */
class Solution {
    fun singleNumber(nums: IntArray): Int {
        TODO("Implement solution")
    }
}
