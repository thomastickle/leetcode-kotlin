package leetcode.p0216

/**
 * # 216. Combination Sum III
 *
 * Find all valid combinations of `k` numbers that sum up to `n` using these rules:
 *
 * - Only numbers from `1` through `9` may be used.
 * - Each number may be used at most once.
 * - The result must not contain the same combination twice.
 *
 * Return a list of all possible valid combinations. The combinations may be returned in any
 * order.
 *
 * Examples:
 * - Input: `k = 3, n = 7`; output: `[[1, 2, 4]]`. The only valid combination is `1 + 2 + 4 = 7`.
 * - Input: `k = 3, n = 9`; output: `[[1, 2, 6], [1, 3, 5], [2, 3, 4]]`. These combinations
 *   respectively sum to `9`, and there are no other valid combinations.
 * - Input: `k = 4, n = 1`; output: `[]`. The smallest sum of four different numbers from `1`
 *   through `9` is `1 + 2 + 3 + 4 = 10`, which is greater than `1`.
 *
 * Constraints:
 * - `2 <= k <= 9`.
 * - `1 <= n <= 60`.
 *
 * [LeetCode 216: Combination Sum III](https://leetcode.com/problems/combination-sum-iii/)
 */
class Solution {
    fun combinationSum3(k: Int, n: Int): List<List<Int>> {
        TODO("Implement solution")
    }
}
