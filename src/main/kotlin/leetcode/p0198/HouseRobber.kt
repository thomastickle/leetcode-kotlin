package leetcode.p0198

/**
 * # 198. House Robber
 *
 * You are a professional robber planning to rob houses along a street. Each house contains a
 * certain amount of money, but adjacent houses have connected security systems. If two adjacent
 * houses are broken into on the same night, the police are automatically contacted.
 *
 * Given an integer array `nums` representing the amount of money in each house, return the maximum
 * amount of money you can rob tonight without alerting the police.
 *
 * Examples:
 * - Input: `nums = [1, 2, 3, 1]`. Output: `4`. Explanation: Rob house 1 (money = 1) and house 3
 *   (money = 3), for a total of `1 + 3 = 4`.
 * - Input: `nums = [2, 7, 9, 3, 1]`. Output: `12`. Explanation: Rob house 1 (money = 2), house 3
 *   (money = 9), and house 5 (money = 1), for a total of `2 + 9 + 1 = 12`.
 *
 * Constraints:
 * - `1 <= nums.length <= 100`.
 * - `0 <= nums[i] <= 400`.
 *
 * [LeetCode 198: House Robber](https://leetcode.com/problems/house-robber/)
 */
class Solution {
    fun rob(nums: IntArray): Int {
        TODO("Implement solution")
    }
}
