package leetcode.p1143

/**
 * # 1143. Longest Common Subsequence
 *
 * Given two strings `text1` and `text2`, return the length of their longest common subsequence.
 * If there is no common subsequence, return `0`.
 *
 * A subsequence is a sequence that can be derived from another sequence by deleting some or no
 * elements without changing the relative order of the remaining elements. For example, `ace` is a
 * subsequence of `abcde` because `b` and `d` can be deleted without changing the order of `a`,
 * `c`, and `e`. A common subsequence is a subsequence that is present in both strings.
 *
 * Examples:
 * - Input: `text1 = "abcde"`, `text2 = "ace"`. Output: `3`. Explanation: The longest common
 *   subsequence is `ace`, which has length `3`.
 * - Input: `text1 = "abc"`, `text2 = "abc"`. Output: `3`. Explanation: The longest common
 *   subsequence is `abc`, which has length `3`.
 * - Input: `text1 = "abc"`, `text2 = "def"`. Output: `0`. Explanation: There is no common
 *   subsequence.
 *
 * Constraints:
 * - `1 <= text1.length, text2.length <= 1000`.
 * - `text1` and `text2` consist of only lowercase English characters.
 *
 * [LeetCode 1143: Longest Common Subsequence](https://leetcode.com/problems/longest-common-subsequence/)
 */
class Solution {
    fun longestCommonSubsequence(text1: String, text2: String): Int {
        TODO("Implement solution")
    }
}
